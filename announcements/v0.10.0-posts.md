# Alloy v0.10.0 Announcement Posts

---

## LinkedIn Post

OpenAI just revealed something most people missed.

Their internal agent orchestrator — the system that generates 1M+ lines of code and processes 1 billion tokens per day — is built on Elixir.

Not Python. Not TypeScript. Elixir.

The model itself chose it. BEAM's process supervision, fault tolerance, and lightweight concurrency are exactly what AI agents need.

This is validation for every Elixir developer who's been saying "OTP already solves this." The patterns Python agent frameworks are building — isolated state, message passing, supervision, fault recovery — Erlang shipped them in 1986.

We've been building Alloy, an open-source agent harness that makes it easy to build AI agents on the BEAM. It's not an orchestrator like Symphony — it's the engine inside each agent. The completion-tool-call loop, provider abstraction, and OTP integration so your agents survive production.

What's new in v0.10.0:

- 6 LLM providers — swap with one line. Claude, GPT, Gemini, Grok, Codex + any OpenAI-compatible API.
- Structured output you can trust — force the model to call a specific tool before it can finish, so you get validated data every time
- Human-in-the-loop that actually works — middleware can inspect AND rewrite tool arguments before execution
- Auto-recovery when context gets too large
- ~7,500 lines with 3 runtime dependencies

If you're building agents that need to stay running in production, the BEAM is where they belong.

github.com/alloy-ex/alloy

#elixir #ai #agents #opensource #otp

---

## X Post (Thread — 4 posts)

**Post 1:**
OpenAI's internal agent orchestrator runs on Elixir. Not Python.

The model chose Elixir for BEAM's process supervision.

We've been building Alloy — the agent engine that makes it easy to build AI agents on the BEAM.

Not an orchestrator. The loop inside each agent.

github.com/alloy-ex/alloy

**Post 2:**
What's new in v0.10.0:

- 6 LLM providers — swap with one line
- `until_tool` — force structured output by requiring a specific tool call before the loop ends
- Middleware can edit tool arguments before execution (real HITL)
- Auto-recovery from context overflow

3 deps. 7,500 lines. MIT.

**Post 3:**
Every Python agent framework is reinventing what Erlang shipped in 1986:

- Isolated state? GenServer.
- Fault recovery? Supervision trees.
- Parallel execution? Task.Supervisor.
- Message passing? That's literally what BEAM does.

Alloy gives you the agent loop. OTP gives you everything else.

**Post 4:**
Why does this matter?

AI agents crash. APIs timeout. Context windows overflow. Models hallucinate tool calls.

On the BEAM, your agent restarts automatically. Tools run in parallel without threading headaches. Context compaction kicks in before you hit the limit.

Build agents that survive production: github.com/alloy-ex/alloy

---

## X Post (Single post version)

OpenAI's agent orchestrator runs on Elixir.

Alloy is the open-source agent engine for the BEAM — the loop inside each agent, not the orchestrator above them.

v0.10.0: 6 providers, structured output enforcement, HITL argument editing, auto context recovery.

3 deps. 7,500 lines. MIT.

github.com/alloy-ex/alloy

---

## ElixirForum Post

**Title:** Alloy v0.10.0 — 6 providers, structured output enforcement, HITL editing

**Body:**

Hey everyone! Quick update on Alloy, the minimal agent harness.

**Context:** OpenAI recently revealed their multi-agent orchestrator (Symphony) is built on Elixir — the model itself chose it for BEAM's supervision capabilities. Symphony is the orchestration layer that coordinates fleets of agents. Alloy operates one level down — it's the engine inside each individual agent. The completion-tool-call loop, provider abstraction, and OTP integration. Different layer, same thesis: the BEAM is a natural fit for AI agent workloads.

**What's new in 0.10.0:**

**3 new providers** — Alloy now supports 6 providers: Anthropic, OpenAI, Gemini, xAI, Codex, and any OpenAI-compatible API (Ollama, DeepSeek, etc.). Swap with one line:

```elixir
# Switch from Claude to Grok — change one line
{:ok, result} = Alloy.run("Summarize this code",
  provider: {Alloy.Provider.XAI,
    api_key: System.get_env("XAI_API_KEY"),
    model: "grok-4"
  },
  tools: [Alloy.Tool.Core.Read]
)
```

**`until_tool` for structured output** — Instead of hoping the model returns valid JSON, define a tool with the exact schema you want and set `until_tool: "submit_answer"`. The loop won't complete until the model calls that tool. The schema is validated at the API level — no parsing, no prayer.

```elixir
{:ok, result} = Alloy.run("Analyze this data",
  provider: {Alloy.Provider.Anthropic, api_key: key, model: "claude-sonnet-4-6"},
  tools: [SubmitAnalysis],
  until_tool: "submit_analysis"
)
```

**HITL `:edit` variant** — Middleware can now return `{:edit, modified_call}` from `:before_tool_call` to rewrite tool arguments before execution. Previously you could only block or allow. Now you can correct a file path, sanitize a command, or enforce policy — without stopping the agent.

```elixir
def call(:before_tool_call, state) do
  call = state.config.context[:current_tool_call]
  if dangerous?(call), do: {:edit, sanitize(call)}, else: state
end
```

**Tool safety** — Tools declare `concurrent?/0` (safe to parallelize?) and `max_result_chars/0` (output cap). The executor handles the rest. Plus automatic recovery when context gets too long — forces compaction and retries instead of failing.

**Numbers:** 568 tests, 0 failures. ~7,500 lines of code. 3 runtime dependencies. MIT license.

Full changelog: https://github.com/alloy-ex/alloy/blob/main/CHANGELOG.md

Feedback welcome — especially on the `until_tool` pattern. Curious how others are handling structured output from agents.

---

## Hacker News Post

**Title:** Alloy: Minimal OTP-native AI agent harness for Elixir (3 deps, 7.5k lines)

**URL:** https://github.com/alloy-ex/alloy

---

## Reddit r/elixir Post

**Title:** OpenAI built their agent orchestrator on Elixir. Here's an open-source agent engine for the BEAM — Alloy v0.10.0

**Body:**

OpenAI's Frontier team revealed their multi-agent orchestrator (Symphony) is built on Elixir. GenServer-per-task, OTP supervision trees, 1B tokens/day. The model chose Elixir for BEAM's process supervision.

Alloy isn't an orchestrator like Symphony — it's one layer down. The engine inside each agent. The completion-tool-call loop, provider abstraction, and OTP integration so your agents survive production.

What it does:

- 6 providers — swap with one line (Anthropic, OpenAI, Gemini, Codex, xAI + any compatible API)
- Each agent is a supervised GenServer with fault isolation
- Parallel tool execution under Task.Supervisor
- Context compaction with structured handoffs when conversations get long
- Cost guards, middleware hooks, streaming

New in v0.10.0:

- `until_tool` — structured output enforcement. The loop won't end until the model calls a specific tool. No more hoping the model returns valid JSON.
- HITL `:edit` — middleware can rewrite tool arguments before execution, not just block them
- Tool safety controls (`concurrent?/0`, `max_result_chars/0`)
- Auto-recovery from prompt-too-long errors
- 3 new providers: Gemini, xAI, Codex

568 tests. 3 runtime deps. ~7,500 lines. MIT.

https://github.com/alloy-ex/alloy

Would love feedback, especially from anyone building agents on the BEAM.
