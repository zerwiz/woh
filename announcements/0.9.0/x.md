Alloy 0.9.0 — the Elixir agent engine now with prompt caching & xAI support

New:
→ Anthropic prompt caching (cache: true) — 60-90% input token savings
→ Reasoning model support — DeepSeek/xAI thinking blocks parsed automatically
→ extra_body — pass any provider param (response_format, temperature, etc.)
→ 6 telemetry events — full run/turn/provider observability
→ grok-4 family with 2M context windows

One line to swap between Claude, GPT, Grok, Gemini. ~5k lines. OTP-native.

hex.pm/packages/alloy
github.com/alloy-ex/alloy
