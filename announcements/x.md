Alloy 0.8.0 — the agent engine for Elixir

Build AI agents that work with any model. Swap between Claude, GPT, Gemini, Grok with one line.

New: cost guard (cap agent spend), smart context compaction (long tasks don't lose context), streaming helper.

4 tools, a loop, OTP supervision. ~5k lines. That's the whole framework.

hex.pm/packages/alloy
github.com/alloy-ex/alloy
