# AlloyAgent configuration
# NOTE: Config temporarily disabled for Elixir 1.14 compatibility testing
# Will be re-enabled once basic compilation works

# config :alloy_agent, AlloyAgent,
#   root_path: "/home/zerwiz/woh",
#   agent_dir: "alloy_agent/definition",
#   default_team: "all",
#   tools: [:read, :write, :edit, :ls, :find, :grep, :bash],
#   default_agents: ["architect", "builder", "scanner", "tester"]